# fm self

Commands to update the Frappe Manager tool itself, update images, or run docker compose commands used by the tool.

Usage examples:

```bash
# Update the CLI (may prompt)
fm self update

# Update images used by benches
fm self update-images

# Pass through to docker compose
fm self compose ps
```
