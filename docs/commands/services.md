# fm services

Start, stop, restart, or open a shell for a named service. Service names include `global-db`, `global-nginx-proxy`, or bench-level services.

Usage:

```console
$ fm services start SERVICE_NAME
```

Examples:

```bash
fm services start global-db
fm services restart global-nginx-proxy
```
